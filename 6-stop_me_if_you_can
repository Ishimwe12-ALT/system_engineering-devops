#!/usr/bin/env bash
# Stops the 4-to_infinity_and_beyond process using gdb, avoiding kill or killall
pid=$(ps -eo pid,cmd | grep '[4]-to_infinity_and_beyond' | grep -v grep | awk '{print $1}')
if [ -n "$pid" ]; then
  gdb -q -n -p "$pid" -batch -ex "call (int) exit(0)" > /dev/null 2>&1
fi
